-- Copy the following text into a new crt_{table}.sql file OR
   Copy/paste the file, change the filename, and change the extensions to .sql
-- Fill in the appropriate data in the table_metadata

{% set table_metadata = {
    "schema_name": "",
    "table_name": "",
    "transient_table": "",
    "table_definition": "

    ",
    "full_refresh_ddl_statements": [

    ]
}%}

-- DON'T REMOVE
{%- set create_table_hook = create_data_mart_table(table_metadata) -%}

{{ config(
    materialized = "table",
    pre_hook = create_table_hook,
    schema = table_metadata.schema_name
)}}


-- Used to create a reference in DBT, REQUIRED DON'T REMOVE.
WITH source AS (
    SELECT * FROM {{ table_metadata.table_name }}
),

-- <REPLACE W/ TRANSFORMATION DESCRIPTION> 
-- TRANSFORMATION PLACE HOLDER. EDIT BELOW 
renamed AS (
    SELECT 
      *
    FROM source
)

SELECT * FROM renamed
